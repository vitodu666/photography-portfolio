# 摄影网站工程交接说明

## 先看这里

- 网页入口：`prototypes/hero-options/index.html`
- 图片来源：每个系列的 `网站展示` 文件夹
- 封面规则：`网站展示` 中名为 `封面` 的图片
- 首屏视频：`图片/首屏视频/`
- 图片同步：`prototypes/hero-options/sync-media-data.mjs`

## 快速预览

在工程包根目录启动一个本地静态服务器，再打开：

`http://127.0.0.1:4174/prototypes/hero-options/index.html`

如果电脑有 Python，可以在工程包根目录运行：

`python3 -m http.server 4174`

不要直接双击 HTML 判断动效或返回路径；本地服务器预览更接近正式网页。

## 给接手 Agent 的一句话

先完整阅读 `网站维护说明.md` 和 `PROJECT_REVIEW.md`，再检查 `prototypes/hero-options/`；不要重做页面结构，不要绕过 `网站展示` 和 `封面` 规则，也不要直接手改自动生成的三个资料文件。

## 平时换图

1. 只在对应系列的 `网站展示` 中增删图片。
2. 入口图命名为 `封面`，建议裁成 2:3 竖图。
3. 运行 `sync-media-data.mjs` 重新生成资料。
4. 检查首页、三个总览、详情页和返回位置。
5. 重新发布，线上链接才会更新。

## 工程包里的图片

交接包中的照片是为网页和传输压缩过的副本，保留了原文件夹、文件名和封面规则，足够继续设计、修改和发布；摄影原片仍以原项目中的图片为准，不要用交接包反向覆盖原片。

## 当前线上版本

- 海外公开地址：`https://du-wentong-photo-portfolio.wentongdu.chatgpt.site`
- 该地址在中国大陆可能无法稳定访问。
- 国内发布时可上传整个解压后的工程包；根目录 `index.html` 会自动进入正式网页入口。

## 当前结构

- L0：首页，展示精选人像、精选商业、日常入口和联系信息。
- L1：人像、商业、日常三个总览页。
- L2：具体系列详情与组图浏览。
- 电脑端人像保留三列速度差；手机端为单列。
- 首页首图读取 5 段视频并轮换播放。

## 不要丢的文件

- `prototypes/hero-options/`：正式网页源文件。
- `图片/`：仅包含网页实际使用的压缩副本和首屏视频。
- `网站维护说明.md`：日常换图和增加系列的方法。
- `PROJECT_REVIEW.md`：设计决策、已知问题和历次修改。
- `文件清单.json`：工程包内文件、大小和校验值。

