# 部署上线 · 腾讯云 COS 静态网站（免费 · 国内 · 免备案）

> ⚠️ **已弃用**：本方案被 `部署上线-EdgeOne-Pages-极简指引.md` 取代。EdgeOne Pages 域名更短、支持 Git 自动部署、更契合"换任何 AI 都能接管"的诉求。保留本文件仅作参考。

> 适用：已确定的方案——国内免费托管 + 不要自有域名（用平台免费二级域名）+ 仅禁用右键不加水印。
> 成本：¥0/年（新用户有免费额度，超出按量几元/月，作品集量级基本用不完）。
> 前提：需要一个**你本人**的腾讯云账号（实名认证免费），这一步 BB 不能替你注册登录。

## 你需要做的（约 5 分钟，全程网页点选，不用写代码）

1. **注册并实名**
   打开 `https://cloud.tencent.com`，用微信扫码注册，按提示做实名认证（免费，几分钟）。

2. **开通 COS 并建存储桶**
   - 控制台搜「对象存储 COS」→ 开通（免费）。
   - 建存储桶：名称随便（如 `duwentong-photo`），地域选**广州/上海/北京**任一，
     「访问权限」选 **公有读私有写**，其余默认 → 确定。

3. **上传网站文件**
   - 进入存储桶 → 「文件列表」。
   - 把本地目录 `photography-portfolio/prototypes/hero-options/` 里的**所有内容**
     （index.html、series.html、各 css/js、图片文件夹等）**直接上传到桶根目录**
     （不要带着 `prototypes/hero-options/` 这层路径，否则网址要多敲子路径）。
   - 可用「上传文件夹」一键传整个目录。

4. **开启静态网站**
   - 存储桶 → 左侧「基础配置」→「静态网站」→ 编辑 → 开启。
   - 索引文档填 `index.html`，错误文档也填 `index.html` → 保存。

5. **拿到你的网址**
   - 开启后页面会显示一个「访问节点」，形如：
     `https://duwentong-photo-xxxxxxxxx.cos-website.ap-guangzhou.myqcloud.com`
   - 这个就是你的网站地址，**国内直接打开、免备案、免费**，转发给别人即可。

## 以后更新内容 / 加作品

- 改完本地文件后，把对应文件在 COS 文件列表里「覆盖上传」即可，网址不变。
- 加一组新作品：按 `网站维护说明.md` 的流程在本地改好数据 → 把新增的图片文件夹和 `portfolio-data.js` 覆盖上传到 COS。

## 备选：嫌上面麻烦，可以让 BB 用 CloudStudio 代部署

如果你不想自己操作腾讯云，可以让 BB 用 WorkBuddy 的 CloudStudio 能力帮你直接发布
（同样国内可访问、免费、有链接）。优点是你说一声就上线；缺点是链接带平台后缀。
需要你确认授权后 BB 再操作。

## 想升级成自己的正式域名（以后再说，非必须）

等哪天想要 `www.你的名字.com` 这种正式网址：
买个 `.com`（≈¥73/年）→ 在腾讯云做 ICP 备案（免费，1–2 周）→ 在 COS 基础配置里绑自定义域名。
现在不用管，先用免费二级域名。
