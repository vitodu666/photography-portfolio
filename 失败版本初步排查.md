# 失败版本排查

查看日期：2026-08-12

页面：`http://127.0.0.1:4173/prototypes/photo-layouts/index.html#campaign`

源文件：`/Users/duwentong/Documents/Codex/Codex- project/美的视觉产生/prototypes/photo-layouts/index.html`

本次已经读完旧任务 `019fdba5-8fe4-74b0-9b14-b1a240afbc79` 的全部对话，并对照检查页面、代码、桌面/手机截图、项目复盘与设计资料。

## 修正后的结论

这张 HTML 不能被简单判成“Agent 把作品集网站做错成评审页”。在旧任务的最后阶段，用户明确提出先用几张图片做几个排版小样，再决定专注哪个板块；页面确实按这个要求做了三个简历图片板块小样。

所以要分开看：

- 对当时最后一个小任务：形式基本对题，技术上也能打开、能适配手机，并修过裁切和溢出。
- 对当时整段协作：方向变化太多，却没有在每次变化后重新说清当前目标、停止旧路线。
- 对现在的摄影作品集网页：不合适，不能作为继续修改的底稿。
- 对审美结果：没有通过。Agent 自己说“结构成立”不能算数；用户没有选择任何一个小样，最后停止了任务。

## 旧任务是怎样走偏的

1. 最初讨论的是：分析编辑型 HTML 作品网页需要哪些能力和工具，不直接做网页。
2. 随后转成：先搭一套长期视觉生产能力。
3. 接着进入 Jiro 案例研究、Skill 制作和运行时安装。
4. 后来任务又换成作品型简历，再缩成 HTML 视觉简历。
5. 最后才缩成三个图片板块小样，形成当前页面。

每一步单看都有用户输入或纠正，但 Agent 没有把旧目标明确作废，也没有给新阶段建立一张简短、唯一的当前说明。结果是同一任务里同时积累了作品网页、能力体系、Jiro 研究、简历系统和图片小样五套上下文。

## 这张页面本身做到了什么

- 三个结构确实不是简单换色：身份开场、作品选集、商业项目各有不同构图。
- 页面和六张已有截图可以正常查看；旧记录显示修过姓名裁切、说明区溢出和人物裁切。
- 390 px 手机宽度没有横向溢出，图片资源正常加载。
- 它适合比较局部排版，不是完整网站，也没有声称自己是完整网站。

## 为什么用户仍然没有得到想要的结果

1. **准备很多，能看的结果太晚。** 大量时间先花在能力系统、案例研究、Skill 和长文档上，真正供用户判断的视觉结果出现得很晚。
2. **参考没有被固定成页面规则。** 旧任务研究了许多案例，但没有在做小样前明确“只由哪个参考控制结构、哪个参考只控制一个局部、哪些手法绝对不用”。
3. **技术检查代替不了审美认可。** 能打开、无报错、无溢出，只证明文件正常；用户没有选中 01/02/03，视觉方向就仍然没有通过。
4. **不同目标的资料混在一起。** `reference-analysis.md` 和 `design-system.md` 服务的是 A4 简历，强调一页一张主图、打印和招聘阅读；这些规则与连续浏览、多图陈列、滚动动效的摄影作品集不是一回事。
5. **旧说明互相冲突。** `direction-proposals.md` 曾写明不用 HTML/CSS 做低精度方向，后续又实际用 HTML/CSS 做三个小样；说明文件没有随任务变化及时作废或更新。

## 用户反思放在什么位置

用户已经反思：此前给了参考，却没有把需求以足够具体的方式固定下来，而且曾较强势地推动制作。这是事实，也确实增加了误解空间。

但这不是唯一原因。Agent 在每次目标变化后都有责任重新确认当前要做的东西、指出旧资料是否还适用，并在用户尚未认可视觉前避免用“已经成立”替代用户判断。

## 现在保留什么

- 保留真实照片和已经验证过的裁切经验。
- 保留“先做小样、用户判断后再扩展”的做法。
- 保留 Pontus Rudolfson 作为作品陈列主参考。
- 只从 Together Group 借动态首屏和大字关系。
- PureVisuals 只作备用实现路线。

## 现在不用什么

- 不沿用黑色评审外壳和三个方案并排的结构。
- 不沿用 A4 简历的固定画布、打印尺寸和一页一主图限制。
- 不把旧 `design-system.md` 当成新作品集的设计规则。
- 不把技术通过写成视觉通过。
- 不再同时混合多套参考风格。

## 当前决定

新网页从空白结构开始，不在旧小样上修补。首轮只验证一条方向：Pontus 控制作品陈列和滚动节奏，Together 只控制首屏动态影像与大字；先出一个可浏览骨架，用户认可后再补完整内容和细节。
