# 网站维护说明

现在不再手动逐张改网页路径。网站的图片清单由 `prototypes/hero-options/sync-media-data.mjs` 根据图片文件夹生成；每次整理好图片后同步一次即可。

> **谁都能跑这个同步**：你（双击 `prototypes/hero-options/同步图片.command`）或任何品牌的 AI Agent（运行 `node sync-media-data.mjs`）都能做，**不绑定某个特定 Agent**。换 Agent 时，把整个 `photography-portfolio` 文件夹 + `HANDOFF.md` + 本文件 + `AGENT_HANDOFF.md` 给它即可，它照文档就能接手。

## 文件夹规则

- 人像：`图片/人像/组号/网站展示/`
- 商业：`图片/产品/组号/网站展示/`
- 日常：`图片/风光/地点/网站展示/`
- 首屏视频：`图片/首屏视频/`

网页只会读取每组 `网站展示` 里的图片，不会读取该组外层或其他临时文件夹里的照片。

## 封面规则

1. 每组必须在 `网站展示` 中放一张名为 `封面` 的图片，扩展名可以是 JPG、JPEG、PNG、WEBP 或 AVIF。
2. 封面负责首页、总览和详情首图，不会在下方组图中重复出现。
3. 封面建议提前裁成 2:3 竖图；横图虽然会被网页裁进 2:3 窗口，但容易损失构图。
4. 组图中的其他图片保留原比例，点进详情后完整展示。

## 平时换图

1. 进入对应系列的 `网站展示`。
2. 删除不再展示的旧图，放入需要展示的新图。
3. 若要换入口封面，把新图命名为 `封面`，并确保同一文件夹只有一张封面。
4. 运行同步（双击 `同步图片.command`，或让 Agent 跑 `sync-media-data.mjs`），同步后检查网页。

不要直接修改自动生成的 `portfolio-data.js`、`commercial-data.js` 和 `hero-media.js`；再次同步时，手动改动会被覆盖。

## 增加新系列

1. 人像或商业新建一个新的数字组号文件夹；日常新建地点文件夹。
2. 在里面新建 `网站展示`。
3. 放入一张 `封面` 和其余组图。
4. 同步后，新系列会自动进入对应总览。

没有封面的空文件夹会被自动跳过。目前 `图片/人像/17/网站展示` 为空，因此没有进入网页。

## 首页和总览的区别

- 首页保留12组精选人像、6组精选商业和1个日常总入口，不会因为新增文件夹无限变长。
- 人像、商业和日常总览会显示所有符合文件夹规则的系列。
- 详情页点 `Back / 返回` 会回到刚才点击的入口；总览页点 `Home / 首页` 才返回首页。

## 首屏视频

- MP4 或 WEBM 放入 `图片/首屏视频/` 后会进入播放清单。
- 视频按文件名顺序连续播放并淡入切换；减少动态效果的设备显示静态封面。
- 建议保持竖版、无声、时长接近，避免单段过大拖慢首屏。

## 修改系列名字

已命名系列和地点的中英文写在 `sync-media-data.mjs` 顶部的名字表中。没有正式名字的新系列暂时显示为 `Portrait 03`、`Commercial 15` 这类中性编号，等名字确定后再统一替换。

## 上线前必须压图（重要）

`网站展示` 里的原图往往很大（单张几 MB 到几十 MB），直接上线会让网页加载很慢。发布前请把组图导出为 **长边 ≤ 2000px、质量 ~82** 的 JPG（或 WebP）覆盖进 `网站展示`；封面建议 2:3 竖图。同步脚本只读取 `网站展示`，压过的版本才会被网页用到。原图母版请另外备份，别只留 `网站展示` 这一份。

## 当前内容

- 人像：21组，首页精选12组。
- 商业：15组，首页精选6组。
- 日常：12组，首页保留一个总入口。
- 首屏视频：5段。

当前检测到3张封面并非接近2:3：产品1约为0.55、产品3约为0.47、产品15为横图1.5；网页仍会使用它们，但会发生裁切。

## 发布后的边界

目前是本地网页。公开发布后，每次换图、增图或改播放视频，都需要重新同步并发布一次，别人打开的网址才会更新。发布方式见 `HANDOFF.md` 第八节 / `AGENT_HANDOFF.md` 第五节。
