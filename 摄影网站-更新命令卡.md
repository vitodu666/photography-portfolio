# 摄影网站 · 更新标准动作（超简命令卡）

> 给「不懂技术的你」和「未来接手的任何 AI」看。加新作品 / 改完内容后，让网站上线就照这个来。

## 你（不懂代码也能做）

1. **放图**：新照片按规矩放进 `图片/人像/组号/网站展示/`（组号用数字，如 `23`；封面图命名 `封面`）。
2. **双击 `同步图片.command`**（在 `prototypes/hero-options/` 里）→ 自动把照片写进网页数据。
3. **双击 `推送更新.command`**（同目录）→ 自动上传，EdgeOne 几秒后自动上线。

完事。不用开命令行、不用懂代码。

## 接手 AI（命令行版）

```bash
cd photography-portfolio
node prototypes/hero-options/sync-media-data.mjs   # 同步数据
git add -A
git commit -m "更新：说明改了啥"
git push                                        # EdgeOne 自动部署
```

## 红线（别踩）

- 改前先压图：长边 ≤ 2000px、质量 ~82，否则线上加载慢。
- 改 CSS/JS 必须递增缓存戳（见 `网站维护说明.md`），否则浏览器不刷新。
- 别手改 `portfolio-data.js` / `commercial-data.js` / `hero-media.js`（自动生成，同步会覆盖）。
- 部署账号、域名、GitHub 全是用户的，**任何 AI 改完 push 即上线，不依赖 BB / WorkBuddy 额度**。

## 首次部署（只做一次，用户自己账号）

1. GitHub 建空仓库（Private）→ 把本地仓库连上去并 push（见 `部署上线-EdgeOne-Pages-极简指引.md`）。
2. EdgeOne Pages 控制台连该 GitHub 仓库，根目录设为 `prototypes/hero-options`，开通免费套餐。
3. 得到 `前缀.edgeonepages.com` 公开网址，转发给别人即可。
